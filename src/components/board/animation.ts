/**
 * How long a checker takes to glide from its old point to its new one, in ms.
 * Shared so the "piece arrived" sound can fire exactly as the glide finishes.
 */
export const CHECKER_MOVE_MS = 260;
